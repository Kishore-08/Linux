# Linux System Administration Assignment

This repository contains a complete Linux system administration assignment implemented on a **fresh Linux system** with **no pre-installed packages**.

All tasks are implemented using **standard Linux tools**, following best practices and with detailed explanations.

---

## 📌 Assignment Objectives

1. Configure SMTP service on localhost
2. Create a user without sudo privileges
3. Create a global custom command (`describe`)
4. Detect and extract a compressed file automatically
5. Restrict permissions on newly created files without using `chmod`
6. Create and manage a custom Linux service (`showtime`)

---

## 🖥️ System Requirements

- OS: Ubuntu / Debian-based Linux
- User: Root or sudo-enabled user
- Internet access (for package installation)

---




